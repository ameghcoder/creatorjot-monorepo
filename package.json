{
    "name": "creatorjot",
    "private": true,
    "scripts": {
        "dev:frontend": "pnpm --filter frontend dev",
        "dev:backend": "pnpm --filter backend dev",
        "build:shared": "pnpm --filter @creatorjot/shared build",
        "build:frontend": "pnpm build:shared && pnpm --filter frontend build",
        "build:backend": "pnpm build:shared && pnpm --filter backend build",
        "build": "pnpm build:shared && pnpm build:backend && pnpm build:frontend",
        "type-check": "pnpm --filter backend type-check && pnpm --filter frontend exec tsc --noEmit"
    },
    "devDependencies": {
        "typescript": "^6.0.2"
    }
}